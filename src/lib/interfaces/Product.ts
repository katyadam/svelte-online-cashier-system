export interface Product {
    id: number;
    name: string;
    price: number;
    currency: string;
}

export interface ProductDto {
    name: string;
    productPlaneId: number;
    price: number;
    currency: string;
}

